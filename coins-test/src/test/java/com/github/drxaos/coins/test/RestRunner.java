package com.github.drxaos.coins.test;

import org.junit.runners.model.InitializationError;

public class RestRunner extends AbstractRunner {
    public RestRunner(Class<?> klass) throws InitializationError {
        super(klass);
    }
}
